import { PriorityNamePlusPipe } from './priority-name-plus.pipe';

describe('PriorityNamePlusPipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityNamePlusPipe();
    expect(pipe).toBeTruthy();
  });
});
